- not done
/ in progress
| done

1 easy, take at most 20 mins
2 medium, take at most an hour
3 hard, could take up to day of work
4 very hard, could take up to a week of work
5 EXTREMELY HARD, could take months of work

/5 interphase 1:
  /2 merge mouse events with connect
  -4 settings (drift, quality...)
    -2 interface
      -1 switch between PC and mobile mode
      -1 drift ammount
      -2 phase Lock/Detect Phase
    -3 behaviour
      -3 actions
        -1 merge bit (on, off, upper, lower, and, or)
        -1 delete mode (locked, single, continuous)
    -3 visual
      -1 disable background blur
      -2 use the old engine (only useful for fancy styling in the future)
      -2 full size preRender (Should Fix The Apple IPad Rendering Issue)
      -1 grid line size (including off)
      -1 scroll sensitivity
      -2 preRaster modes (Off/Adaptive/Fixed)
        -1 preRaster Quality
    -1 tick
      -1 disable catch up
      -1 realtime sync
        -1 show control
  /4 efficient blueprints in better format than JSON (keep the code for JSON format! will be required later)
  /4 mobile support (Ả̶̛G̵͑͘H̸͂̚Ḧ̶̈́Ḧ̸́̍)
  -4 proper title screen (secretly keep original meme one and make it accessible somehow)
  -4 puzzles
  
  done:
    | add events
    | merge canvas.onmousemove with canvas.ontouchmove
    | change connections to {upperType, lowerType, upperFlipped, lowerFlipped} rather than {upperType, lowerType, flipped, mixed}
    | change cell type.lowerType to just type.lower and same for upper
    | text tutorial and documentation
      | add X to help menu
      | fade blur
      | make image sizes absolute
      | remove ./ from hyperlink href
      | home page to link to all others
    | fix homeDrift
    | Delete Recovery Mode
    | Switch back to using the camel case "OrCells" rather than "Orcells"
    | music basics
    | add more comments
    | sort everything into sections for their objects
    | add loading screen
    | make images rasterize as they load
    | make the scale variable only update when scaling
    | put all variables and functions into main objects
    | make ticks stop when there is nothing to simulate
    | change bit to a boolean
    | remove static cell property
    | render optimisation
    | tickrate control
    | replace rendering engine with putImageData version

-? phase 3:
  -4 debug cell (type 3)

- interphase 2:
  -3 idle tick optimisation
  - interactive tutorial
  - phase 1&2 video tutorial
  - advanced music
    - find better more rounded icon
    - fix media play button
    - add loading icon
    - expandable menu
      - volume control
      - seek
      - next and previous
      - see current track metadata
      - don't play this track again
      - track list
  - eggs benedict support
  - accounts
    - sign in with google
    - sync Settings
  - cloud blueprints
  - blueprint sharing
  
  - premium
    - download boards in JSON format
    - connect tool (ribbon cables & pathfinding)
    - selections (copy, cut, paste, move, flip, rotate)
      - save selections as components to import and use later
      - upload, share and download components
    - unlimited board size
    - lightning mode (cells won't render but will run faster)
    - fancy styles like cells fading or wiping from where the input came from
    - undo/redo (only required because of powerful tools above)

- strategy mode?